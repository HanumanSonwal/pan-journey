export const DEFAULT_GUEST_VALUE = {
  rooms: 1,
  adults: 2,
  children: 0,
  childAges: [],
  pets: false,
};

export const CHILD_AGES = Array.from({ length: 17 }, (_, index) => index + 1);

export const MAX_ROOMS = 20;
export const MAX_ADULTS = 40;
export const MAX_CHILDREN = 40;
